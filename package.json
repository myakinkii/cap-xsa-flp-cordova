{
  "name": "fitrepublic",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "dependencies": {
    "@sap/cds": "^3.34.2",
    "@sap/cds-odata-v2-adapter-proxy": "^1.4.33",
    "@sap/hana-client": "^2.5.86",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "^10 || ^12"
  },
  "scripts": {
    "build": "cds build",
    "deploy": "cds deploy",
    "start": "cds run",
    "local": "cds deploy --to sqlite:my.db --no-save && CDS_ENV=local cds serve all"
  },
  "cds": {
    "[local]": {
      "requires": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "database": "my.db"
          }
        }
      }
    },
    "requires": {
      "db": {
        "kind": "hana",
        "model": [
          "db",
          "srv"
        ]
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ],
  "devDependencies": {
    "sqlite3": "^5.0.0"
  }
}